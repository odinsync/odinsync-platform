/**
 * Application layer for the Identity bounded context.
 *
 * <p>Coordinates use cases such as registration, login, refresh-token rotation,
 * logout, and session management. Application services depend on ports and
 * domain models, keeping persistence and framework details behind adapters.
 */
package com.odinsync.identity.application;
